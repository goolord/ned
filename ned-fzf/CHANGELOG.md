# Changelog

## 0.1.0.0

First release: `Ned.Fuzzy` over the bundled fzf matcher, with
`matchCandidates` scanning a whole candidate arena in one call and
`matchPositions` reporting matched characters for highlighting.
